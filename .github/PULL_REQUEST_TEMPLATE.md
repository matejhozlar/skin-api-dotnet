## What and why

<!-- A short description of the change and the motivation behind it. -->

## Linked issue

<!-- e.g. Closes #123. Delete if not applicable. -->

## Checklist

- [ ] Branched off `dev` and this PR targets `dev`.
- [ ] `dotnet build -c Release` is clean (warnings are errors in this repo).
- [ ] `dotnet test -c Release` passes, with tests added or updated for the change.
- [ ] Public API additions carry XML doc comments.
- [ ] `KnownPoses.cs` was not hand-edited (it is generated by `tools/GeneratePoses`).
- [ ] Commits follow `type(scope): description`.
